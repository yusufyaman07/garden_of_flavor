// Import the initApp function from the helpers.js file
import initApp from "./helpers.js";

// Add an event listener that runs the initApp function
// once the DOM content has fully loaded
document.addEventListener("DOMContentLoaded", initApp);
